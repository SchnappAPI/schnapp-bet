# Handoff — 2026-05-05-16

## Session summary

Implemented Phase 3: logistic model fitting per market group. Three commits shipped:

- `faef8e6` — Phase 3 core: MARKET_GROUP_MAP, 7 features, ensure_grade_weights_table(),
  fetch_grade_corpus(), assemble_features(), _fit_logistic() (scipy L-BFGS-B), write_grade_weights(),
  fit_logistic_models(), main() integration
- `1cf415b` — Bugfix: get_engine() was hardcoding TrustServerCertificate=no; now reads
  SQL_TRUST_CERT from env. This had been breaking the weekly calibration since May 3.
- `1e6fe78` — Guard: fit_logistic_models wrapped in try/except so isotonic calibration
  path can't fail due to schema gaps or empty corpus

Weekly calibration workflow ran successfully. Isotonic calibration: 4902 rows, 15 buckets,
cap 0.625. Logistic fitting: 0-row corpus (expected — Phase 2 graded data hasn't resolved yet;
model will train once games from today onward finish). common.grade_weights table created.

bootstrap.sql regeneration is pending — gen_ddl.py was blocked by the safety hook. Run it
manually: `SQL_PASS=$(grep MSSQL_SA_PASSWORD /Users/schnapp/sql-server.env | cut -d= -f2) && SQL_SERVER="localhost,1433" SQL_DATABASE="sports-modeling" SQL_USERNAME="sa" SQL_PASSWORD="$SQL_PASS" python /tmp/gen_ddl.py`
Then commit the updated database/_shared/bootstrap.sql.

## Where to resume

**Start at Phase 4: Shadow backtest in calibration.**

Phase 4 adds a chronological train/holdout split to weekly_calibration.py:
- Train window: days 1–30
- Holdout window: days 31–37
- Score = sum(ev_pct × 1.0) for holdout props where model_prob > implied_prob (flat $1 stake)
- Write weights only if candidate_score > production_score
- Populate holdout_score, production_score on grade_weights rows
- Add candidate_score, production_score, weights_updated, market_group to common.calibration_history

NOTE: Phase 4 requires model_prob to evaluate ev_pct on the holdout. Since Phase 5 hasn't
shipped yet (model_prob isn't in daily_grades), the holdout score needs to be computed inline
during the calibration run rather than read from the DB. The shadow backtest fits the candidate
model, computes model_prob → ev_pct on holdout rows directly, then decides whether to write.

## Phases status

| Phase | Status |
|---|---|
| 1: Usage rate ETL + player_usage_stats | COMPLETE |
| 2: Relevance-weighted hit rate | COMPLETE |
| 3: Logistic model fitting + grade_weights | COMPLETE |
| 4: Shadow backtest in calibration | NOT STARTED |
| 5: Model Prob, EV%, outcome resolution fix | NOT STARTED |
| 6: KDE tier lines replacement | NOT STARTED |
| 7: Model performance comparison table | NOT STARTED |
| 8: Web display | NOT STARTED |
| 9: Re-run today's grading | NOT STARTED |

## Key decisions this session

- Used scipy.optimize.minimize (L-BFGS-B) for logistic fitting instead of sklearn — avoids
  adding a dependency to the venv (which doesn't install from requirements.txt)
- Logistic corpus filter: `relevance_hit_rate IS NOT NULL AND effective_n IS NOT NULL AND
  outcome IN ('Won','Lost')` — this means only Phase 2+ graded resolved games contribute
- Table DDL is inline in weekly_calibration.py via ensure_grade_weights_table(), not in
  bootstrap.sql directly (bootstrap.sql requires gen_ddl.py regeneration)
- try/except around fit_logistic_models is intentional — it's additive to the critical
  isotonic path; a logistic failure should never block the calibration from publishing

## Context that would be lost without this handoff

- The bootstrap.sql regeneration needs to be done manually (gen_ddl.py blocked by hook)
- Phase 4 shadow backtest must compute model_prob inline from the candidate weights, not
  from DB, because Phase 5 (model_prob in daily_grades) hasn't shipped yet
- Once Phase 4 ships, the try/except guard in main() can be tightened — logistic failures
  in a mature system should surface rather than be swallowed
- The ANTHROPIC_API_KEY and CLAUDE_CODE_OAUTH_TOKEN from earlier sessions still need rotation
  at console.anthropic.com / claude.ai settings

## Files to read at next session start

1. `grading-v2/plan.md`
2. `grading-v2/memory.md`
3. This file
4. `grading/weekly_calibration.py` (skim fit_logistic_models and write_grade_weights for context)
