# Handoff — 2026-05-05-19

## Session summary

Implemented Phase 6: KDE tier lines replacement with EV%-based player_value_lines table.

Commit: `281c6f2`

Changes in `grading/grade_props.py`:
- `ensure_tables()`: added `common.player_value_lines` DDL — 22-column table, unique on (grade_date, game_id, player_id, market_key, line_value)
- `upsert_value_lines(engine, rows)`: new function after `upsert_tier_lines`; dedup on 5-column key, same temp-table + batch INSERT + MERGE pattern
- `grade_props_for_date()`: post-loop pass builds `grade_rows_by_pmg` index (positive-EV Over grade rows by (player_id, market_key, game_id)); iterates dict items for opportunity context + hit stats, emits one value row per positive-EV line; return changed from 2-tuple to 3-tuple `(grade_rows, tier_rows, value_rows)`
- `run_upcoming/run_intraday/run_backfill`: unpack 3-tuple; call `validate_and_filter(value_rows, "common.player_value_lines", ...)` + `upsert_value_lines(engine, value_rows)`
- `run_backfill` force=True skip clause updated to check `common.player_value_lines` (not `player_tier_lines`)
- `MODEL_VERSION` bumped to `"grading-v2.0"`
- `compute_kde_tier_lines()` preserved (MLB uses it); `tier_rows` always empty post-Phase 6

Grading CI run 25395853718 succeeded:
- Schema verified (player_value_lines DDL applied)
- 1005 grade rows written
- 0 value line rows (expected — no active grade_weights yet; logistic corpus still accumulating)

## Where to resume

**Start Phase 7: Model performance comparison table.**

Phase 7 adds `common.model_performance` to `grading/weekly_calibration.py` — written on each calibration run with snapshot_date, model_version, market_group, n_resolved, hit_rate, avg_ev_pct, profit_weighted_score, composite_grade_correlation.

Phase 8 (web display) and Phase 9 (re-run grading) follow.

## Pending items

- `opp_ratio_minutes` column not yet stored in daily_grades — logistic model uses fillna(1.0) neutral fallback (intentional)
- `web/public/sw.js` has an unstaged modification — investigate before next web deploy
- ANTHROPIC_API_KEY and CLAUDE_CODE_OAUTH_TOKEN still need rotation
- Value lines will appear in player_value_lines once calibration runs with a sufficient resolved-grade corpus (Phase 2 grades accumulate over ~2 weeks of games)

## Phases status

| Phase | Status |
|---|---|
| 1: Usage rate ETL + player_usage_stats | COMPLETE |
| 2: Relevance-weighted hit rate | COMPLETE |
| 3: Logistic model fitting + grade_weights | COMPLETE |
| 4: Shadow backtest in calibration | COMPLETE |
| 5: Model Prob, EV%, outcome resolution fix | COMPLETE |
| 6: KDE tier lines replacement | COMPLETE |
| 7: Model performance comparison table | NOT STARTED |
| 8: Web display | NOT STARTED |
| 9: Re-run today's grading | NOT STARTED |

## Files to read at next session start

1. `grading-v2/plan.md`
2. `grading-v2/memory.md`
3. This file
4. `grading/weekly_calibration.py` — skim `fit_logistic_models()`, `write_grade_weights()`, and `main()` before writing Phase 7 code
