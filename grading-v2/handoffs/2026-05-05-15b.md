# Handoff — 2026-05-05-15b

## Session summary

Built Phases 1 and 2 of grading-v2. Phases 3–9 remain. Context window is getting long — continue in a fresh chat.

## Commits this session

- `ddb7318` — Phase 1: player_usage_stats ETL (boxscoreadvancedv3, _parse_iso_minutes, load_player_usage_stats)
- `e857766` — Phase 2: relevance-weighted hit rate (fetch_player_role_context, _relevance_weight, compute_relevance_weighted_hit_rate, compute_all_hit_rates extended, _common_grade_data now returns 8-tuple)
- `a11a6df` — plan.md updated

## Where to resume

**Start at Phase 3: logistic model fitting in weekly_calibration.py.**

Phase 3 needs to:
1. Add `common.grade_weights` table DDL to weekly_calibration.py's ensure step
2. Pull resolved corpus (same as existing fetch_resolved_corpus) but add features: relevance_hit_rate, effective_n, momentum_grade, pattern_grade, hit_rate_opp, role_volatility, opportunity signals from opp_df
3. For each market group (Volume/Rate/Counting), fit sklearn LogisticRegression on train set
4. Write coefficients to common.grade_weights
5. grade_props.py reads common.grade_weights at startup, falls back to default weights if missing
6. Compute model_prob = logistic sigmoid of (features @ coefficients + intercept) at grade time

Phase 4: shadow backtest — train days 1–30, holdout days 31–37, profit-weighted score comparison

Phase 5: model_prob, implied_prob, ev_pct columns on daily_grades + outcome resolution fix (>= instead of >)

Phase 6: KDE tier lines replaced with neg-binomial/normal + EV% per posted line

Phase 7: common.model_performance table, written by weekly_calibration.py

Phase 8: Web display — queries.ts, grades route, GameTabs PropMatrix/PropsTab, At a Glance

Phase 9: trigger refresh-data

## Critical facts for Phase 3

- `weekly_calibration.py` already has `fetch_resolved_corpus()` which pulls from player_tier_lines joined to daily_grades outcomes. This is the RIGHT corpus to calibrate the KDE. For the logistic model, we need daily_grades joined to outcomes directly (not through tier lines), because we want per-prop grades not per-tier predictions.
- The resolved corpus for logistic fitting: `SELECT dg.relevance_hit_rate, dg.effective_n, dg.momentum_grade, dg.pattern_grade, dg.hit_rate_opp, dg.role_volatility, dg.market_key, CASE WHEN dg.outcome='Won' THEN 1 ELSE 0 END as hit FROM common.daily_grades dg WHERE dg.outcome IN ('Won','Lost') AND dg.grade_date >= DATEADD(day, -37, GETDATE())`
- Market group classification: Volume = {player_points, player_pra, player_pr, player_pa, player_ra and _alternate variants}, Rate = {player_threes, player_steals, player_blocks and _alternate}, Counting = {player_rebounds, player_assists and _alternate}
- sklearn is NOT in the venv. Must add it: `/Users/schnapp/venv/bin/pip install scikit-learn` and add to grading/requirements.txt
- Default weights (fallback when common.grade_weights is missing): logistic intercept=-1.0, relevance_hit_rate coefficient=3.0, momentum_grade=0.5, pattern_grade=0.3, effective_n_log=0.1, hit_rate_opp=1.0, role_volatility=-0.2
- MODEL_VERSION for v2: `grading-v2.0`

## Critical facts for Phase 5

- implied_prob formula: price > 0: 100/(price+100); price < 0: abs(price)/(abs(price)+100)
- ev_pct formula: (model_prob * payout - (1 - model_prob)) * 100 where payout = price/100 (positive) or 100/abs(price) (negative)
- Outcome resolution fix: change `stat > line_value` to `stat >= line_value` for Over, `stat < line_value` to `stat <= line_value` for Under in run_outcomes UPDATE SET CASE WHEN block

## Critical facts for Phase 8

- At a Glance component lives in web/lib/queries.ts (getGrades function) and rendered somewhere in web/app
- PropsTab fetches from /api/grades route
- Sort by EV% descending. Show only EV% >= 0 by default, with filter to show all
- Model Prob shown as percentage (e.g. 68%), EV% shown with sign (e.g. +14.2% or -3.1%)
- Color: EV% > 8% green, 0-8% yellow, <0 gray (text-gray-600, not hidden)

## ETL status

nba-etl.yml was triggered at 15:05 UTC. Was still in_progress at last check. Should complete within ~5 minutes. Will create nba.player_usage_stats and begin backfilling. 10 game dates per run, ~120 total dates needed for full regular season backfill. Daily ETL will continue automatically.

## Do not do in next session

- Do not rewrite historical grades
- Do not change MODEL_VERSION until Phase 5 is complete and all new columns are wired in
- Do not add scikit-learn without also adding it to grading/requirements.txt
- Do not forget to update _common_grade_data if adding new fetches — it already returns 8-tuple, any new data fetches for Phase 3 features should go there or be fetched inline in weekly_calibration.py (not _common_grade_data, which is for daily grading only)
