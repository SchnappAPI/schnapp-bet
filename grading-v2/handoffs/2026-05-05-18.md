# Handoff — 2026-05-05-18

## Session summary

Implemented Phase 5: model_prob/implied_prob/ev_pct wiring + outcome resolution fix.

Commits:
- `6b25ec6` — Phase 5 code in grading/grade_props.py (121 insertions, 10 deletions)
- `50ecb69` — bootstrap.sql regeneration capturing Phase 5 schema + earlier phases

Changes in `grading/grade_props.py`:
- Added `from scipy.special import expit` import
- Added LOGISTIC_FEATURE_NAMES, LOGISTIC_MARKET_GROUP_MAP, LOGISTIC_OPP_SHRINKAGE_K,
  LOGISTIC_ROLE_VOL_SCALE constants (duplicated from weekly_calibration.py — no shared module)
- Added `load_grade_weights(engine)`: reads common.grade_weights WHERE is_active=1,
  returns dict[group → (coef_array, intercept)] in LOGISTIC_FEATURE_NAMES order;
  returns {} if table missing or no active rows (empty corpus path)
- `ensure_tables()`: added model_prob/implied_prob/ev_pct ADD-COLUMN guard
- `upsert_grades()`: added 3 columns to #stage_grades DDL (38 total), INSERT (38 ?),
  MERGE UPDATE SET, MERGE INSERT column list and VALUES
- `grade_props_for_date()`: calls load_grade_weights(engine) before the row loop;
  inline EV computation uses opp_ratio_minutes=1.0 neutral fallback
- Outcome resolution: >= for Over Won, <= for Under Won (exact hits count)

Also fixed /tmp/gen_ddl.py to use SQL_* env vars instead of AZURE_SQL_*. bootstrap.sql
regenerated and committed — common.daily_grades now shows model_prob/implied_prob/ev_pct,
and common.grade_weights/calibration_history tables appear for the first time.

Grading workflow ran successfully on commit 6b25ec6. DDL migration applied via ensure_tables.

## Where to resume

**Start Phase 6: KDE tier lines replacement.**

Phase 6 replaces `compute_kde_tier_lines()` with a neg-binomial/normal fit and replaces
Safe/Value/HighRisk/Lotto thresholds with EV%-based value lines. Emit value line rows
only where ev_pct > 0.

Phase 7 (model performance comparison table) and Phase 8 (web display) follow.

## Pending items

- `opp_ratio_minutes` column not yet stored in daily_grades — all grade rows have NULL,
  the logistic model uses fillna(1.0), which the current inline computation hard-codes.
  Wire in a future phase once the value is computed (probably from matchup/opponent data).
- common.calibration_history and common.grade_weights are created dynamically by
  weekly_calibration.py. They now appear in bootstrap.sql (_shared/bootstrap.sql).
- web/public/sw.js has an unstaged modification (present since before Phase 4) —
  investigate before next web deploy.
- ANTHROPIC_API_KEY and CLAUDE_CODE_OAUTH_TOKEN still need rotation at
  console.anthropic.com / claude.ai settings.

## Phases status

| Phase | Status |
|---|---|
| 1: Usage rate ETL + player_usage_stats | COMPLETE |
| 2: Relevance-weighted hit rate | COMPLETE |
| 3: Logistic model fitting + grade_weights | COMPLETE |
| 4: Shadow backtest in calibration | COMPLETE |
| 5: Model Prob, EV%, outcome resolution fix | COMPLETE |
| 6: KDE tier lines replacement | NOT STARTED |
| 7: Model performance comparison table | NOT STARTED |
| 8: Web display | NOT STARTED |
| 9: Re-run today's grading | NOT STARTED |

## Files to read at next session start

1. `grading-v2/plan.md`
2. `grading-v2/memory.md`
3. This file
4. `grading/grade_props.py` — skim `compute_kde_tier_lines`, `grade_props_for_date` tier
   line section, and `upsert_tiers` before writing any Phase 6 code
