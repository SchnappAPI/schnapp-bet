# Handoff — 2026-05-05-17

## Session summary

Implemented Phase 4: shadow backtest gate for logistic weight updates. One commit shipped:

- `12e36fc` — Phase 4 core: BACKTEST_TRAIN_DAYS=30 / BACKTEST_HOLDOUT_DAYS=7 constants;
  `_implied_prob()` (vectorized American odds → implied prob); `score_holdout()` (inline
  feature engineering + model_prob → ev_pct, flat $1 stake, sum over model_prob > implied);
  `get_production_score()` (loads active grade_weights, re-scores same holdout, 0.0 on first
  run so candidate always wins on a fresh install); `ensure_calibration_log_table()` /
  `write_calibration_log()` (common.calibration_history DDL + per-run upsert);
  `write_grade_weights()` extended with holdout_score/production_score kwargs;
  `fit_logistic_models()` fully rewritten with 37-day corpus, chronological split, champion/
  challenger gate (write only if candidate > production); main() try/except tightened to
  log.error + raise so logistic failures surface in CI rather than being silently swallowed.

Workflow ran successfully on the new code. 0-row corpus path executed cleanly:
"Backtest corpus: 0 resolved rows over 37 days" → "Empty corpus; skipping logistic fitting"
→ "Weekly calibration done." No errors.

## Where to resume

**Start at Phase 5: Model Prob, EV%, outcome resolution fix.**

Phase 5 adds three columns to `common.daily_grades` and wires them into `grade_props.py`:
- `model_prob FLOAT` — logistic model output at grade time, loaded from common.grade_weights
- `implied_prob FLOAT` — derived from over_price using American odds → prob formula
- `ev_pct FLOAT` — (model_prob × payout - (1 - model_prob)) × 100

Also fixes outcome resolution: stat >= line = Won for Over (was stat > line), stat <= line
= Won for Under. Exact hits on whole-number lines count as Won.

Key implementation path in `grade_props.py`:
1. `load_grade_weights(engine)` — cache active weights per market_group from grade_weights;
   return dict[group → (coef_array, intercept)] using LOGISTIC_FEATURE_NAMES order.
   Return empty dict if grade_weights has no active rows (skip silently — corpus is still empty).
2. At grade time in `precompute_line_grades`, after computing relevance_hit_rate features:
   - compute model_prob = expit(features @ coef + intercept) using the loaded weights for
     the market group; NULL if no weights available for this group
   - compute implied_prob from over_price (same formula as _implied_prob in weekly_calibration)
   - compute ev_pct from model_prob + implied_prob
3. `ensure_tables()` / `upsert_grades()`: add model_prob, implied_prob, ev_pct to the DDL
   and MERGE statement
4. Outcome resolution fix: change `> line` to `>= line` for Over, `< line` to `<= line`
   for Under in the resolution pass (grep for `outcome_resolution` or `Won` in grade_props.py
   to locate the exact spot)

NOTE: `_common_grade_data` returns a 7-tuple — never change its arity (memory.md invariant).
NOTE: IDENTITY columns (grade_id, tier_id) must NOT be in always_required in validate_and_filter.

## Phases status

| Phase | Status |
|---|---|
| 1: Usage rate ETL + player_usage_stats | COMPLETE |
| 2: Relevance-weighted hit rate | COMPLETE |
| 3: Logistic model fitting + grade_weights | COMPLETE |
| 4: Shadow backtest in calibration | COMPLETE |
| 5: Model Prob, EV%, outcome resolution fix | NOT STARTED |
| 6: KDE tier lines replacement | NOT STARTED |
| 7: Model performance comparison table | NOT STARTED |
| 8: Web display | NOT STARTED |
| 9: Re-run today's grading | NOT STARTED |

## Key decisions this session

- `score_holdout` inlines the feature engineering rather than calling `assemble_features`,
  so `over_price` stays in scope without changing the train-path signature. Training pop_mean
  is passed explicitly to avoid leaking holdout statistics into opponent shrinkage.
- First-run behavior: `get_production_score` returns 0.0 when grade_weights has no active
  rows, so the candidate always wins on a fresh DB and weights are written immediately.
- `write_calibration_log` uses DELETE+INSERT (idempotent on snapshot_date + market_group)
  so re-running the calibration workflow on the same day is safe.
- try/except in main() now re-raises after logging, surfacing logistic failures in CI. The
  handoff for Phase 3 noted this guard could be tightened once Phase 4 shipped — done.

## Context that would be lost without this handoff

- The logistic corpus is still 0 rows. Phase 4 code handles this gracefully. Once Phase 2
  graded games from today onward resolve, the corpus grows and the backtest gate becomes
  active. No action needed.
- common.calibration_history is created on first run by ensure_calibration_log_table().
  bootstrap.sql regeneration is still pending (gen_ddl.py spawn task from last session —
  check if it ran before touching bootstrap.sql again).
- Phase 5 also needs to add model_prob/implied_prob/ev_pct to the supplemental generator
  (grading/generate_supplemental.py) if those columns are used in the prop target filter.
  Investigate when starting Phase 5.
- The ANTHROPIC_API_KEY and CLAUDE_CODE_OAUTH_TOKEN from earlier sessions still need
  rotation at console.anthropic.com / claude.ai settings.

## Files to read at next session start

1. `grading-v2/plan.md`
2. `grading-v2/memory.md`
3. This file
4. `grading/grade_props.py` — skim `precompute_line_grades`, `_common_grade_data`,
   `upsert_grades`, and the outcome resolution section before writing any Phase 5 code
