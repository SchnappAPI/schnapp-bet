# database/

DDL documentation for the local SQL Server 2022 container (`localhost,1433`, database `sports-modeling`).

Invariants and rules live in `.claude/rules/database.md` (auto-loaded when editing files under `database/`). For schema layout, regeneration, and operational procedure, see `database/README.md` and `/skill regenerate-bootstrap-sql`.
