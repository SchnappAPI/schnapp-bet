# CHANGELOG 2026

Newest entries at the top. Tag taxonomy and format documented in `docs/changelog/README.md`.

## 2026-05-17 [etl][odds] Port etl/odds_etl.py from sports-modeling

Port-as-is. 2,009 lines, verbatim copy. Five modes: `discover`, `backfill`, `mappings`, `upcoming`, `probe`. FanDuel-only invariant preserved — `BOOKMAKERS = "fanduel"` constant at line 128 passed to every `/odds` and `/events` request (5 call sites). Imports from `shared.db` (`get_engine`, `upsert`) and `shared.integrity` (`validate_and_filter`, `ensure_tables`). `python3 -c "import ast; ast.parse(...)"` passes. First real-code test of `.claude/rules/etl.md` — FanDuel-only and PYTHONPATH lines apply.

## 2026-05-17 [services][flask] Port services/flask/runner.py from sports-modeling

Port-as-is. 189 lines, stateless Flask proxy to the NBA CDN. Three routes: `/ping` (no auth), `/scoreboard` (X-Runner-Key), `/boxscore?gameId=` (X-Runner-Key). Zero coupling to repo (stdlib + `requests` + `flask` only). Bind `0.0.0.0:5000`. Run by launchd user agent `bet.schnapp.flask`.

## 2026-05-17 [shared] Port shared/integrity.py from sports-modeling

Port-as-is. 1,175 lines, three-layer data integrity framework per ADR-20260424-2. CRITICAL_FIELDS catalog (8 NBA tables + 7 odds tables + 5 common tables), RELATIONAL_CHECKS catalog (lineup coverage, box-score completeness, mapped events, schedule/player staleness, quarantine spike, player counts), DDL_STATEMENTS for the three framework tables, `validate_and_filter`, `retroactive_scan`, `record_unmapped_entity`, `generate_health_report`. Per-sport CRITICAL_FIELDS split deferred to the NBA pipeline port. Doc references in source updated from `/docs/DECISIONS.md ADR-20260424-2` to `docs/decisions/ADR-20260424-2-data-integrity-framework.md` to match the new ADR layout.

## 2026-05-17 [shared] Port shared/db.py from sports-modeling

Port-as-is. 126 lines. Five exports: `_build_conn_str`, `get_engine` (fast*executemany=True default, 3 retries × 45s), `get_engine_slow` (fast_executemany=False; required for long-VARCHAR staging and NVARCHAR(MAX)), `record_workflow_run` (idempotent table create + MERGE), `upsert` (DataFrame → `#stage*{table}`→ MERGE). Env-driven via`SQL_SERVER`, `SQL_DATABASE`, `SQL_USERNAME`, `SQL_PASSWORD`, `SQL_TRUST_CERT`. ODBC Driver 18 for SQL Server.

## 2026-05-17 [meta][docs] Simplify host-runtime guidance

Tightened the wording in `CLAUDE.md`, `.claude/rules/etl.md`, `.claude/rules/workflows.md`, `docs/CONNECTIONS.md`, and `docs/decisions/ADR-20260517-2-scaffolding-milestone.md` so the rules and identity state the operational answer plainly: Python runs on mac-runner or via Mac MCP `shell_exec`. Removed parenthetical host-specific reasoning that did not change the rule.

## 2026-05-17 [shared][docs][meta] Scaffolding milestone complete

Structure-and-scaffolding rebuild from sports-modeling shipped to `/Users/schnapp/code/schnapp-bet`. Adds `.claude/bootstrap-plugins.sh` plus 17-plugin declaration in `settings.json`, 4 new path-scoped rules (`database.md`, `flask.md`, `workflows.md`, `docs.md`), 5 new skills (`regenerate-bootstrap-sql`, `regenerate-health`, `new-sport-onboarding`, `changelog-rotate`, `live-session-cache`), 1 new command (`adr.md`). Shrinks `database/CLAUDE.md` and `services/flask/CLAUDE.md` to thin pointers. Builds out `docs/` with router, PRODUCT_BLUEPRINT, CONNECTIONS, GLOSSARY, ROADMAP, changelog/, decisions/ (10 ported ADRs + 2 new), features/playoff-supplemental, and 4 runbooks. Adds `.gitignore` and component README stubs. Drops `grading-v2/`, `infrastructure/`, `memory/`, `docs/skills/`, etl/\_shared/ from the scope. See `docs/decisions/ADR-20260517-2-scaffolding-milestone.md` and `docs/decisions/ADR-20260517-1-bootstrap-strategy.md`.
