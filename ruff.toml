# Deliberately lenient starting point: only the fatal classes (syntax errors,
# undefined names, misused f-strings/comparisons). Tighten select as the
# codebase is consolidated — do not loosen it.
line-length = 120
target-version = "py312"

[lint]
select = ["E9", "F63", "F7", "F82", "F821"]
